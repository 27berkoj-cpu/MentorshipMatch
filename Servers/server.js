// MentorshipMatch backend entry point.
//
// Build this file in the following order:
// 1. Load environment variables and import Express.
// 2. Create the Express application.
// 3. Add JSON parsing middleware so req.body works for JSON requests.
// 4. Serve the frontend files so the browser can load the website.
// 5. Register API routes such as /api/auth, /api/mentors, and /api/sessions.
// 6. Add a final error-handling middleware function.
// 7. Start listening on a port from process.env.PORT or a local default.
//
// Keep database code in a separate database module. This file should connect
// the pieces together rather than contain every query in the application.

